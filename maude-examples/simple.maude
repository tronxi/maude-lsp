fmod SimpleModule is
    sorts Nat .
    op zero : -> Nat [ctor] .
    op succ : Nat -> Nat [ctor] .
    op add : Nat Nat -> Nat .
    vars X Y : Nat .
    eq add(zero, X) = X .
    eq add(succ(X), Y) = succ(add(X, Y)) .
endfm